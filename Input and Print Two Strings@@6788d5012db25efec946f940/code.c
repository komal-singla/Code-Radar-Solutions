#include <stdio.h>
int main(){
    string a,b;
    scanf("%s",&a);
    scanf("%s",&b);
    // printf("You entered: %s %s",a,"and",b);
    printf("You entered: %s %s",a,"and",b);
    return 0;

}