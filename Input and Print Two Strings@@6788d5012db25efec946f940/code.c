#include <stdio.h>
int main(){
    string a;
    scanf("%s",&a);
    // scanf("%s",&b);
    // printf("You entered: %s %s",a,"and",b);
    printf("You entered: %s",a,"and",b);
    return 0;

}